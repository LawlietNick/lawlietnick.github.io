---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
  {
    date.toLocaleDateString("fi-FI", {
      month: "short",
      day: "numeric",
      year: "numeric",
    })
  }
</time>
